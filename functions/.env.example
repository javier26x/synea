# Copia este archivo a functions/.env y rellena los valores de tu SMTP.
# La contraseña NO va aquí: se guarda como secret con:
#   firebase functions:secrets:set SMTP_PASSWORD

SMTP_HOST=mail.frody.cl
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=synea@frody.cl
FROM_NAME=Synea Studio Spa
